# TODO

- [ ] Update Angular SSR routing so parameterized client routes render on the client instead of forced server rendering.
- [ ] Make authGuard SSR-safe (don’t evaluate login state during server render).
- [ ] Verify /upload/123 and /claims/5 load components (no blank screen).

